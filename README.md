# knn-mnist-from-scratch
A custom implementation of the k-Nearest Neighbors (k-NN) algorithm from scratch using PyTorch to classify the MNIST dataset, featuring custom Priority Queue algorithms and PyTorch tensor broadcasting for performance optimization.
